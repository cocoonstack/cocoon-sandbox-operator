apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: sandbox-operator
  namespace: {{ include "agent-sandbox.namespace" . }}
  labels:
    {{- include "agent-sandbox.labels" . | nindent 4 }}
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
- apiGroups:
  - ""
  resourceNames:
  - sandbox-webhook-certs
  resources:
  - secrets
  verbs:
  - get
  - patch
  - update
